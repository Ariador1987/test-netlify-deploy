import { dev } from '$app/environment';

export const title = 'Shakespeare';
export const description = 'Sveltekit Blog for poets';
export const url = dev ? 'http://localhost:5173' : 'url';
// mdsvex is a Preprocessor which treats markdown files as Components so its able to turn everything into HTML
